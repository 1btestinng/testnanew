import Link from 'next/link';
import { MARKETS } from '@/lib/markets';

export default function Home() {
  return <div className="page"><section className="hero"><div><p className="eyebrow">FINANCIAL MARKET INTELLIGENCE</p><h1>Markets, companies, and data — without the noise.</h1><p className="lede">A multi-country market intelligence platform built around normalized data, transparent calculations, and explicit unavailable states.</p></div><Link className="primary-button" href="/markets">Explore markets</Link></section><section><div className="section-head"><div><p className="eyebrow">MARKETS</p><h2>Start with a market</h2></div><span className="muted">Provider-backed · country aware</span></div><div className="market-grid">{Object.values(MARKETS).map(m => <Link href={`/markets/${m.country}`} className="market-card" key={m.country}><div><span className="country-code">{m.country}</span><h3>{m.countryName}</h3></div><div className="market-meta"><span>{m.exchangeCode}</span><span>{m.currency}</span></div></Link>)}</div></section><section className="principles"><div><span className="principle-no">01</span><h3>No fabricated numbers</h3><p>Missing provider data stays missing. Derived values are explicitly identified.</p></div><div><span className="principle-no">02</span><h3>Historical FX matters</h3><p>Historical USD values use the FX observation corresponding to the historical date.</p></div><div><span className="principle-no">03</span><h3>Built to expand</h3><p>Country configuration and provider adapters keep the application shared and scalable.</p></div></section></div>;
}
